```bash
git init
git add server.py requirements.txt
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/marsoftmedia/rekat-price-server.git
git push -u origin main
```
